# Run artefacts: predictions, metrics, adapters, checkpoints. Regenerable, and
# large. The numbers that matter are copied into docs/JOURNAL.md by hand, so the
# record survives without the weights.
runs/

# Weights that are megabytes or gigabytes. The logistic head is neither: it is
# about 3k floats, it is the deliverable, and it is committed in model/.
models/hf/
models/gguf/
*.gguf
*.safetensors

__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

.DS_Store
